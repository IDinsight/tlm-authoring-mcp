---
description: Construire ou faire évoluer le programme — standards, composantes, cours, leçons, regroupements
argument-hint: [ce qu'il faut construire — un objectif, un cours, des leçons, un regroupement ; à la main, depuis une recherche ou depuis un fichier]
---

Build or change the curriculum: $ARGUMENTS

Follow the `construire-le-programme` skill. Read `get_graph_guide` first and look at what already
exists (`namespace_stats`, `find_node`, `walk_graph` in skeleton) before creating anything: a second
standards spine is worse than none.

Get the proposal from the source I name — my words, a search of the graph, or a file read by the
`lecteur` subagent — then follow the shared write procedure in `session-autorat`: resolve names in
one call, one batched write per level, show me the change in French before you confirm.

Never ask me for an identifier. Check the wiring with `check_draft` after each level.
