---
description: Créer ou faire évoluer un document — ce qu'il couvre, sa mise en forme, ses grilles, ses sections, son journal
argument-hint: [le document — un guide, un livre, un cahier ; ce qu'il couvre ; à la main, sur un modèle ou depuis un fichier]
---

Compose or change a document: $ARGUMENTS

Follow the `composer-un-document` skill and its order: the document with what it covers by name
(`create_document`), a formatter that carries a `render` bag, the grids, then the sections
(`add_section`), each naming what it covers.

Get the proposal from the source I name — my words, an existing document as a model, or a file read
by the `lecteur` subagent — then follow the shared write procedure in `session-autorat`. Show me the
structure in French before you write it.

Every decision that is not obvious gets a dated entry in the document's or the section's journal.
Do not produce the file from here; that is `produire-et-mesurer`.
