---
description: Build a new course from nothing, in the right order
argument-hint: [the course — grade, subject, what it covers]
---

I want to build a new course: $ARGUMENTS

Follow the `nouveau-cours` skill and keep to its order — course root, standards spine, routines from
the catalog, the document, its formatter, then its sections. Each step attaches to the last, and out
of order leaves elements nothing can reach.

Read `get_graph_guide` first, and check `namespace_stats` before creating anything: some of this
usually exists already, and a second standards spine is worse than none.

Prefer adapting a catalog routine over authoring one from scratch. Show me each step before you
write it.
