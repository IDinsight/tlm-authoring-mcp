---
description: Construire un nouveau cours à partir de rien, dans le bon ordre
argument-hint: [le cours — niveau, matière, ce qu'il couvre]
---

I want to build a new course: $ARGUMENTS

Follow the `construire-le-programme` skill for the curriculum — standards, components, the course,
its groupings and lessons, the routine from the catalog — then `composer-un-document` for the
document, its formatter and its sections. Each step attaches to the last, and out of order leaves
elements nothing can reach.

Read `get_graph_guide` first, and check `namespace_stats` before creating anything: some of this
usually exists already, and a second standards spine is worse than none.

Prefer adapting a catalog routine over authoring one from scratch. Show me each step before you
write it.
